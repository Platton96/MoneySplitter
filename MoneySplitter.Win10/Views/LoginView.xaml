<Page
    x:Class="MoneySplitter.Win10.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneySplitter.Win10.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal"
                                               >
                <TextBlock Text="Email"
                           />
                <TextBox PlaceholderText="Enter email"
                         Text="{x:Bind ViewModel.Email, Mode=OneWay}"
                         />
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Margin="5">
                <TextBlock Text="Pasword"/>
                <PasswordBox PlaceholderText="Enter Password"
                             Password="{x:Bind ViewModel.Password, Mode=OneWay}"/>
                
            </StackPanel>
            <Button Content="Login"
                    Click="OnLoginClick"/>
        </StackPanel>
    </Grid>
</Page>
